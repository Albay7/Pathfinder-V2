name: App Build & Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      APP_NAME: ${{ secrets.AZURE_WEBAPP_NAME }}
      RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: dom, curl, mbstring, zip, pdo, mysql, gd, bcmath
          coverage: none

      - name: Composer install (prod)
        run: |
          composer install --no-dev --prefer-dist --no-interaction --no-progress
          php artisan key:generate --ansi || true
          php artisan config:clear || true

      - name: Node install & build
        run: |
          npm ci || npm install
          npm run build || npm run prod || echo "No build script"

      - name: Archive app for deploy
        run: |
          mkdir -p build
          tar -czf build/app.tar.gz --exclude=.git --exclude=build .

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Azure context info
        run: |
          az account show --output table
          az group show -n "$RESOURCE_GROUP" --output table || true

      - name: Preflight: verify Web App exists
        run: |
          if [ -z "$APP_NAME" ] || [ -z "$RESOURCE_GROUP" ]; then
            echo "Missing required secrets: AZURE_WEBAPP_NAME or AZURE_RESOURCE_GROUP";
            exit 1;
          fi
          echo "Checking web app $APP_NAME in resource group $RESOURCE_GROUP";
          if ! az webapp show -n "$APP_NAME" -g "$RESOURCE_GROUP" >/dev/null 2>&1; then
            echo "Web App $APP_NAME not found in resource group $RESOURCE_GROUP.";
            echo "Ensure the app exists and the service principal in AZURE_CREDENTIALS has access to this subscription.";
            exit 1;
          fi

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          package: build/app.tar.gz

      - name: Smoke test
        run: |
          echo "Hit health endpoint";
          curl -I "https://${{ secrets.AZURE_WEBAPP_NAME }}.azurewebsites.net/simple-health.php" || true